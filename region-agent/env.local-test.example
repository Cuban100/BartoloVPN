# For docker-compose.local-test.yml only - a throwaway "remote" region
# simulated on this dev host. Copy to .env.local-test before use.
#
#   cp env.local-test.example .env.local-test
#   docker compose -f docker-compose.local-test.yml --env-file .env.local-test up -d

AGENT_API_KEY=local-test-key-not-for-real-use-00000000000000
WIREGUARD_SUBNET=10.13.13.0

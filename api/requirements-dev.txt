# Test-only dependencies, not needed at runtime.
pytest
pytest-asyncio
# starlette==0.27.0 (pinned via fastapi==0.104.1) relies on TestClient's
# "app=" shortcut, which httpx removed in 0.28 - pin below that.
httpx<0.28
